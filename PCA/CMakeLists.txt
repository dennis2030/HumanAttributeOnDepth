cmake_minimum_required(VERSION 2.8)
set(project_name PCA)
project( ${project_name} )
find_package( OpenCV REQUIRED )
add_executable( ${project_name} PCA.cpp )
target_link_libraries( ${project_name} ${OpenCV_LIBS} )
